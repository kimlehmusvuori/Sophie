"""LLM prompt templates. Pure text-formatting only — never a source of raw
health/calendar data (that boundary lives in the callers assembling the
already-sanitized context objects passed in here)."""
