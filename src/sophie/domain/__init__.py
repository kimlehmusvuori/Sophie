"""Pure domain logic: types and deterministic calculations. No I/O, no
framework imports (no SQLAlchemy sessions, no Streamlit, no httpx) — this is
the layer that stays true across any future frontend/backend swap."""
